# Use system linker when available, fall back to rust-lld for musl targets
[target.x86_64-unknown-linux-gnu]
# linker = "rust-lld"  # disabled — rust-lld needs system libraries not available here

[target.x86_64-unknown-linux-musl]
linker = "rust-lld"
